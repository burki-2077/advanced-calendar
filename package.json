{
  "name": "advanced-calendar-for-jira",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "cd static && npm run build",
    "deploy": "forge deploy",
    "install:all": "npm install && cd static && npm install"
  },
  "keywords": [],
  "author": "Burak Erol",
  "license": "MIT",
  "description": "Advanced calendar view for Jira work items, events, and schedules",
  "dependencies": {
    "@forge/api": "^6.4.3",
    "@forge/resolver": "^1.7.1"
  },
  "devDependencies": {
    "@forge/cli": "^11.6.0",
    "@forge/tunnel": "^5.10.3"
  }
}
